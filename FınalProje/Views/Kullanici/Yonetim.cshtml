@model List<FınalProje.ViewModels.KullaniciViewModel>

@{
    ViewData["Title"] = "Kullanıcı Yönetimi";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Kullanıcı Yönetim Paneli</h2>
        <a asp-action="Create" class="btn btn-success">+ Yeni Kullanıcı Ekle</a>
    </div>

    <table class="table table-hover table-bordered shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Adı</th>
                <th>Soyadı</th>
                <th style="width: 200px;">İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.KullaniciID</td>
                    <td><strong>@item.Ad</strong></td>
                    <td><strong>@item.Soyad</strong></td>
                    <td>
                        <div class="btn-group" role="group">
                            <a asp-action="Edit" asp-route-id="@item.KullaniciID" class="btn btn-warning btn-sm">Düzenle</a>
                            <button type="button" class="btn btn-danger" onclick="silmeIslemi(@item.KullaniciID)">
                                Sil
                            </button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="mt-3">
        <a asp-action="Index" class="btn btn-secondary">← Kategori Sayfasına Dön</a>
    </div>
</div>
@section Scripts {
    <script>
        function silmeIslemi(id) {
            // 1. Onay kutusunu çıkart
            if (confirm("Bu kullanıcıyı silmek istediğinize emin misiniz?")) {

                // 2. Arka planda silme isteğini gönder
                $.ajax({
                    type: "POST",
                    url: "/Kullanici/Delete", // Controller adınız farklıysa burayı güncelleyin
                    data: { id: id },
                    success: function (response) {
                        if (response.success) {
                            // 3. İŞLEM BAŞARILIYSA SAYFAYI YENİLE
                            location.reload();
                        } else {
                            alert("Hata: " + response.message);
                        }
                    },
                    error: function () {
                        alert("Sunucuyla bağlantı kurulamadı!");
                    }
                });
            }
        }
    </script>
}