@model FınalProje.ViewModels.KullaniciViewModel

@{
    ViewData["Title"] = "Yeni Kullanıcı Ekle";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-0" style="border-radius: 15px;">
                <div class="card-header bg-dark text-white text-center" style="border-radius: 15px 15px 0 0;">
                    <h3 class="m-0" style="font-weight: 700; color:white;">Yeni Kullanıcı Ekle</h3>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="form-group mb-4">
                            <label asp-for="Ad" class="form-label" style="font-weight: 600;">Kullanıcı Adı</label>
                            <input asp-for="Ad" class="form-control form-control-lg" style="border-radius: 8px;" placeholder="Ad giriniz..." />
                            <span asp-validation-for="Ad" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-4">
                            <label asp-for="Soyad" class="form-label" style="font-weight: 600;">Kullanıcı Soyadı</label>
                            <input asp-for="Soyad" class="form-control form-control-lg" style="border-radius: 8px;" placeholder="Soyad giriniz..." />
                            <span asp-validation-for="Soyad" class="text-danger"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-success btn-lg" style="border-radius: 8px; font-weight: 600;">
                                Kullanıcıyı Kaydet
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary" style="border-radius: 8px;">
                                Listeye Dön
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}